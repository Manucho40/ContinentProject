// Creation de l'interface ContinentTypes qui nous permettra de typer les données que nous traitons.
export interface ContinentTypes {
    id: number;
    name: string;
    code: string;
    flag: string
}